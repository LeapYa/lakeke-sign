# 这个仓库的脚本（.sh / .py）全部在 Linux 容器里跑。
# ⚠️ 本机 git 开了 core.autocrlf=true，checkout 时会把 .sh 转成 CRLF ——
#    拷进容器就会报 `$'\r': command not found`，而且很难查。
#    这里强制文本文件一律 LF，跟仓库里存的一致（index 里本来就是 LF，所以不产生额外 diff）。
* text=auto eol=lf

# 二进制：别当文本处理
*.png binary
*.jpg binary
*.ico binary
*.deb binary
